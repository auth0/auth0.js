machine:
  node:
    version: v6.10.0
test:
  pre:
    - npm run build
    - npm run lint
  override:
    - npm run ci:test:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml
  post:
    - npm run ci:coverage
general:
  artifacts:
    - build
    - reports
